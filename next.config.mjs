/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "c2.staticflickr.com",
      "lh3.googleusercontent.com",
      "utfs.io",
    ],
  },
};

export default nextConfig;
